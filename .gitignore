# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Environment / secrets
.env
.env.local
*.env
*.pem
*.key
secrets/

# Node (if needed)
node_modules/

# OS
.DS_Store
Thumbs.db

# VSCode
.vscode/settings.json

# Claude Code local machine config — personal, never commit
.claude/settings.local.json

# Project-specific local-only directories — never commit
# (customer/sample docs in collateral/; generated drafts in output/)
collateral/
output/

# Local-only files — backlog, scratch pads, meta notes — never commit
TODO.md
*-conversation.md
CLAUDE.local.md
learnings.md
